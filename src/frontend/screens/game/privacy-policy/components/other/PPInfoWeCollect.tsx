import React from 'react';
import {PolicySection} from '../../../../../common/components';

const PPInfoWeCollect = () => {
  return (
    <PolicySection
      title="What Information Do We Collect?"
      content={`In order for our Service to function correctly we collect (a) the email of those using our Service (b) aggregate information of those using our Service (c) information voluntarily give by the user (such as a survey) (d) address and postal code of the user (used solely to ship prizes to the user) and (e) information related to the use of the mobile application including Internet Protocol (“IP”) address, device name, operating system version, the configuration of the app when utilizing our Service, the time and date of your use of the Service, reaction to advertisements and other such statistics.`}
    />
  );
};

export default PPInfoWeCollect;
